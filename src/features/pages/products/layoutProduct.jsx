import React from 'react';
import { Outlet } from 'react-router-dom';

const LayoutProduct = () => {
    return (
        <>
            <Outlet />
        </>
    );
};

export default LayoutProduct;
